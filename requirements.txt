# Core (versions verified on the reproduction cluster)
torch==2.9.0
torchvision==0.24.0
numpy==2.2.6
hydra-core==1.3.2
omegaconf==2.3.0
lpips
einops
imageio[ffmpeg]
matplotlib
pillow
tqdm
filelock
scikit-learn
opencv-python
trimesh
open3d              # ICP / point-cloud eval (CO3D Chamfer)

# Gaussian-Splatting CUDA rasterizer (build from the official repo):
#   pip install git+https://github.com/graphdeco-inria/diff-gaussian-rasterization
# Plus the EXTERNAL generator repos (not pip-installable; clone and point config.py at them):
#   - diffae      (https://github.com/phizaz/diffae)         -> DIFFAE_ROOT
#   - stylegan3   (https://github.com/NVlabs/stylegan3)      -> STYLEGAN3_ROOT
#   - gs2mesh     (for CO3D Chamfer eval only)               -> used by tools/eval
#   - CATSplat    (RE10K lifter, Table 3)                    -> CATSPLAT_ROOT
# Rebuttal baseline comparisons (tools/rebuttal/baselines) additionally need, at their
# released commits with released weights, under $BASELINES_ROOT:
#   - nerf-from-image (BRFI), 3D-GAN-Inversion (EG3D+PTI), pi-GAN, FINV, EqM
#
# NOTE: `python tools/make_tables.py` and `python tools/rebuttal/report.py` need NONE of
# the above -- they run on the Python 3 standard library alone.
