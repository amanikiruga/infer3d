torch==2.9.0
torchvision==0.24.0
numpy==2.2.6
scipy==1.16.3
opencv-python==4.12.0.88
pillow==11.3.0
imageio[ffmpeg]==2.37.2
einops==0.8.1
lpips==0.1.4
hydra-core==1.3.2
omegaconf==2.3.0
tqdm==4.67.1
filelock==3.19.1
pandas==2.3.3
scikit-learn==1.7.2
matplotlib==3.10.7

# Chamfer distance: registration, ICP and PLY io
open3d==0.19.0
plyfile==1.1.2

# Imported unconditionally by the training/eval loops. Always run with
# WANDB_MODE=disabled (see README); no account is needed.
wandb==0.22.3

# Optional: the ICP evaluator can dump interactive before/after point clouds
plotly==6.4.0

# Two more are NOT installable from PyPI and are covered in README.md step 2:
#   diff-gaussian-rasterization   the CUDA splatting rasterizer (build from source)
#   pytorch3d                     used for ICP in the CO3D evaluator
